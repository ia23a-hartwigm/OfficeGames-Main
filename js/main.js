let swiperBaskets = new Swiper('.home__swiper', {
    // Optional parameters
    direction: 'vertical',
    loop: true,
    spaceBetween: 32,
    grabCursor: true,
  
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },
  
  })